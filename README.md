# mbedOS
Arm mbed operating system.


Compile library as it's written in the link below.
https://github.com/ARMmbed/mbed-cli

or

if you have already a python virtual env.
cd (project)
source ~/Envs/mbed/bin/activate
mbed compile -m K64F -t GCC_ARM --library
